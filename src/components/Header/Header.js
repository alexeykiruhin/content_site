import React from "react";
import './Header.css';

const Header = (props) => {
    return (
        <div className={'Header'}>
            <div className={'logo'}>Contentable</div>
        </div>
    )
}

export default Header;